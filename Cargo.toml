[package]
name = "filepick"
version = "0.1.0"
edition = "2024"

[dependencies]
rfd = "0.17.2"

[features]
no_terminal = []
default_copy = []
always_copy = ["default_copy"]

[dev-dependencies]
tempfile = "3.27.0"
